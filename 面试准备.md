# 自习室座位预约系统 — 面试准备文档

---

## 项目概述（30秒版本）

用 Spring Boot 3.5 + MyBatis-Plus + MySQL + Redis + Spring Security + JWT 做的自习室座位预约后端系统。支持学生查看自习室和预约座位，管理员管理自习室。核心解决了三个问题：并发预约不超卖（乐观锁）、超时未签到自动回收（定时任务）、列表查询性能优化（Redis 缓存）。

---

## 技术架构（面试官问"项目怎么分层的"）

```
controller  → 接收请求，缓存注解，权限校验
service     → 业务逻辑，事务管理
mapper      → MyBatis-Plus BaseMapper，数据库操作
entity      → 实体类映射
dto         → 请求/响应对象
config      → SecurityConfig、RedisConfig、MybatisPlusConfig
security    → JwtAuthenticationFilter
task        → ReservationTimeoutTask（定时回收）
common      → Result 统一响应、GlobalExceptionHandler
```

4 张表：`user`、`study_room`、`time_slot`、`reservation`

---

## 核心功能与技术实现

### 1. 乐观锁防超卖（面试必问）

**问题场景**：10 个人同时抢最后一个座位，如果直接查库存 → 判断 → 扣库存，会出现超卖。

**解决方式**：

```java
// MyBatis-Plus 乐观锁更新
LambdaUpdateWrapper<StudyRoom> updateWrapper = new LambdaUpdateWrapper<>();
updateWrapper.eq(StudyRoom::getId, roomId)
        .gt(StudyRoom::getAvailableCapacity, 0)   // 还有名额
        .eq(StudyRoom::getVersion, room.getVersion()) // 版本号没变
        .setSql("available_capacity = available_capacity - 1")
        .setSql("version = version + 1");

int affected = studyRoomMapper.update(null, updateWrapper);
if (affected == 0) {
    throw new RuntimeException("名额已满，请重试");
}
```

**关键点**：
- 用 `setSql` 而不是 `set`，因为 `setSql` 是 SQL 层面原子操作，读的是数据库当前值
- `UpdateWrapper` 四条件：id 锁定行 + capacity > 0 + version 匹配 + 原子更新
- `@Transactional` 保证扣库存和插入预约记录在同一事务
- 用 `CyclicBarrier` 写并发测试，10 线程抢 5 名额，结果恰好 5 成功 5 失败

### 2. RBAC 角色权限

**实现流程**：

```
注册(role=student) → 登录 → JWT 含 role claim
→ JwtAuthenticationFilter 解析 role → ROLE_ADMIN/ROLE_STUDENT
→ @PreAuthorize("hasRole('ADMIN')") 保护管理接口
```

**遇到的问题**：`@PreAuthorize("hasRole('ADMIN')")` 对管理员也返回 403。

**排查过程**：打印 JWT payload → role 为 `admin` → `SimpleGrantedAuthority("ROLE_" + role)` 拼成 `ROLE_admin` → `hasRole('ADMIN')` 查的是 `ROLE_ADMIN` → **大小写不匹配**。

**解决**：加 `.toUpperCase()`。

### 3. Redis 缓存

**缓存策略**：`@Cacheable` 缓存自习室列表，`@CacheEvict` 增删改时清缓存，TTL 5 分钟。

**遇到的问题（重点讲）**：缓存注解写了没报错，但 Redis 始终为空。

**排查过程**：
1. 确认 Redis 连接正常（`PONG`）
2. 确认 `CacheManager` Bean 被创建
3. 在 Controller 上写了一个简单的 `@Cacheable` 返回 String → 能写入 Redis
4. 对比发现：返回 `Result<String>` 可行，返回 `Result<List<RoomResponse>>` 不可行
5. 调用 `listRooms()` 接口看响应 → `Could not write JSON: Java 8 date/time type java.time.LocalDateTime not supported`
6. 根因：`GenericJackson2JsonRedisSerializer` 内部 ObjectMapper 没有 `JavaTimeModule`，序列化 `RoomResponse` 中的 `LocalDateTime` 时抛异常，但**异常被 CacheInterceptor 静默吞掉了**，日志只显示 "Creating cache entry" 却没有报错

**解决**：创建自定义 ObjectMapper 并注册 `JavaTimeModule`，传入 `GenericJackson2JsonRedisSerializer`。

**另外发现**：`@Cacheable` 不能和 `@Transactional` 放在同一个 Service 类上，因为两个 AOP 代理会冲突。最终把缓存注解放在 Controller 层。

### 4. 定时回收超时预约

**实现**：

```java
@Scheduled(fixedRate = 300000) // 每5分钟
@Transactional
public void cancelTimeoutReservations() {
    LocalDateTime deadline = LocalDateTime.now().minusMinutes(30);
    // 查 status='booked' 且 create_time < 30分钟前
    List<Reservation> timeoutList = reservationMapper.selectList(queryWrapper);
    for (Reservation r : timeoutList) {
        r.setStatus("cancelled");
        reservationMapper.updateById(r);
        // 恢复库存
        studyRoomMapper.update(null, 
            new LambdaUpdateWrapper<StudyRoom>()
                .eq(StudyRoom::getId, r.getRoomId())
                .setSql("available_capacity = available_capacity + 1"));
    }
}
```

### 5. 全局异常处理

**遇到的问题**：Service 层抛 `RuntimeException("自习室不存在")`，前端收到的是空 body 403，看不出任何错误信息。

**排查**：Spring Security 的 `ExceptionTranslationFilter` 会把 filter chain 内部的所有异常（包括非安全异常）都转成 403。没有 `@RestControllerAdvice` 兜底，业务异常就丢了。

**解决**：

```java
@RestControllerAdvice
public class GlobalExceptionHandler {
    @ExceptionHandler(AccessDeniedException.class)
    public Result<Void> handleAccessDenied() {
        return Result.error(403, "权限不足");
    }
    @ExceptionHandler(RuntimeException.class)
    public Result<Void> handleRuntimeException(RuntimeException e) {
        return Result.error(400, e.getMessage());
    }
}
```

---

## 踩坑记录（重点，面试官喜欢听）

| 问题 | 现象 | 根因 | 解决 |
|------|------|------|------|
| POST 请求 403 | GET/DELETE 正常，POST/PUT 返回 403 | curl 终端中文编码损坏，JSON 解析失败被 Spring Security 转成 403 | 用文件传 JSON（`-d @file.json`） |
| `@Version` 不生效 | PUT 更新报 `MP_OPTLOCK_VERSION_ORIGINAL` | `@Version` 需要 `MybatisPlusInterceptor` 配合，只写注解没配拦截器 | 新建 `MybatisPlusConfig`，注册 `OptimisticLockerInnerInterceptor` |
| 管理员权限不生效 | `@PreAuthorize` 对所有人都返回 403 | 数据库 `admin` 是小写，`hasRole('ADMIN')` 查的是 `ROLE_ADMIN` | `role.toUpperCase()` |
| Redis 缓存不写入 | 缓存注解生效但 Redis 为空 | `GenericJackson2JsonRedisSerializer` 缺少 `JavaTimeModule`，序列化 `LocalDateTime` 失败被静默吞掉 | 注册 `JavaTimeModule` 到 ObjectMapper |
| 启动端口冲突 | IDEA 启动失败 | 之前 `nohup` 后台启动的进程没关 | 测试完关进程 |
| `@Transactional` + `@Cacheable` 冲突 | 缓存注解在 Service 层不生效 | 两个 AOP 代理优先级相同，事务代理影响缓存操作 | 缓存注解移到 Controller 层 |
| 新注册用户登录报错 | `role.toUpperCase()` NPE | `register()` 没设 `user.setRole("student")`，role 为 null | 注册时设默认角色 |
| MyBatis-Plus 乐观锁更新失败 | 更新返回 0 行 | `ConcurrentHashMapCacheManager` 每次请求新建缓存 | 加 `@Primary` 和 `CachingConfigurer` 显式指定 |

---

## 可能会问的面试题

**Q: 为什么用 MyBatis-Plus 而不是 JPA？**

A: MyBatis-Plus 对 SQL 控制更精细，乐观锁场景需要手写 WHERE 条件（`version = ? AND available_capacity > 0`），用 JPA 的 `@Version` 只能做版本号判断不能加额外条件。而且 MyBatis-Plus 的 `LambdaUpdateWrapper` 用方法引用写字段名，改名时 IDE 自动感知。

**Q: 为什么不用分布式锁（Redis setnx）而用乐观锁？**

A: 我这个项目是单机部署的演示项目，乐观锁足够。实际生产环境中，如果部署了多实例，乐观锁仍然有效（因为最终锁在数据库行级锁上），而 Redis 分布式锁需要额外运维。乐观锁的缺点是冲突时需要重试，但预约不是高频操作，可接受。

**Q: JWT token 过期了怎么办？**

A: 当前实现没有 refresh token 机制，token 过期（7天）需要重新登录。生产环境通常会加 refresh token 或者使用 OAuth2。

**Q: 为什么缓存放 Controller 层而不是 Service 层？**

A: 因为 Service 层同时有 `@Transactional`，两个 AOP 代理会冲突。实际排查时发现 `@Cacheable` 在 Service 层能拦截但写入 Redis 失败。移到 Controller 层后一切正常。如果一定要放 Service 层，可以通过调整 advisor 优先级（`@EnableCaching(order = ...)`）来解决。

**Q: 定时任务在集群环境怎么办？**

A: 当前是单机 `@Scheduled`。集群环境下多台机器同时跑会重复取消。生产环境通常用分布式任务调度（如 XXL-JOB）或者加分布式锁保证只有一个实例执行。

**Q: 怎么测试并发不超卖的？**

A: 用 JUnit + `CyclicBarrier`。10 个线程在起跑线等待，同时出发抢 5 个名额。验证成功数 ≤ 5 且数据库库存扣减量 = 成功数。代码在 `ReservationServiceTest.java`。

**Q: 项目最难的问题是什么？**

A: Redis 缓存不写入的问题。现象很奇怪——注解写了、CacheManager 有、不报错，但 Redis 就是空的。通过对照实验（Controller 简单缓存 vs Service 复杂缓存）和逐层排查（检查 Redis 连接 → 检查 CacheManager → 检查序列化器 → 发现 LocalDateTime 序列化异常被静默吞掉），最终定位到 `JavaTimeModule` 缺失。这个排查过程让我对 Spring 缓存抽象层的运作机制有了更深入的理解。

---

## 面试自我介绍（1分钟版）

面试官你好，我叫赵登南，沈阳化工大学计算机科学与技术大三学生，应聘 Java 后端开发实习。

技术方面，我熟练掌握 Java 基础和 Spring Boot 全家桶，包括 MyBatis-Plus、Spring Security、JWT、Redis 等。最近独立完成了一个自习室座位预约系统，从数据库设计到接口开发全流程自己做的。

项目里我重点解决了三个问题：用乐观锁防止并发超卖、用 JWT + RBAC 做角色权限隔离、用 Redis 缓存优化查询性能。开发过程中遇到了一些实际问题，比如 Spring Security 吞异常、缓存序列化失败、代理冲突等，都通过排查和查阅文档解决了。

另外我参加过一些学科竞赛，拿过国家级一等奖和两个省级二等奖，主要负责 Java 后端开发。

希望能有机会在贵公司实习，参与真实项目，快速成长。
